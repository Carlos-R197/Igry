<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Igry.Views.SettingsPage"
             Title="More"
             BackgroundColor="White">
    <ContentPage.Content>
        <Grid ColumnDefinitions="*, *" RowDefinitions="*" >
            <StackLayout Grid.Column="0" VerticalOptions="Center" HorizontalOptions="Center">
                <ImageButton Source="logout.png" Aspect="AspectFit" BackgroundColor="Transparent" Command="{Binding LogOutCommand}"/>
                <Label Text="Log out" HorizontalOptions="Center" Style="{StaticResource TitleLabel}"/>
            </StackLayout>
            <StackLayout Grid.Column="1" VerticalOptions="Center" HorizontalOptions="Center">
                <ImageButton Source="RAWGAPI.png" Aspect="AspectFit" BackgroundColor="Transparent" Command="{Binding GoToRawgApiCommand}"/>
                <Label Text="RAWG API" HorizontalOptions="Center" Style="{StaticResource TitleLabel}"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>